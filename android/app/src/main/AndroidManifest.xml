<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Lớp phủ đòi mã: nhờ quyền này mà lớp phủ hiện được đè lên mọi thứ,
         kể cả màn hình chính, sau khi người dùng mở khoá điện thoại. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Đồng bộ với Google Apps Script Web App -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Tự tải và mở trình cài đặt khi có bản mới. Android không cho app tự cài
         đè im lặng, nên đây là mức gần nhất: người dùng chỉ bấm Cài đặt một lần. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/ten_day_du"
        android:supportsRtl="false"
        android:theme="@style/GiaoDien">

        <activity
            android:name=".ChinhActivity"
            android:exported="true"
            android:label="@string/ten_day_du"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/loi_tat" />
        </activity>

        <!-- stopWithTask=false: vuốt app khỏi danh sách gần đây thì dịch vụ
             gác giờ vẫn phải sống. Cùng với onTaskRemoved dựng lại, app không
             bị tắt bằng một cú vuốt. -->
        <service
            android:name=".DichVuKhoa"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Khoa man hinh theo thoi gian bieu da hen" />
        </service>

        <!-- Cấp tệp APK vừa tải cho trình cài đặt. Tự viết thay vì dùng
             FileProvider của AndroidX, để app không phụ thuộc thư viện ngoài. -->
        <provider
            android:name=".NhaCungCapApk"
            android:authorities="com.thoigianbieu.kiemsoat.tep"
            android:exported="false"
            android:grantUriPermissions="true" />

        <receiver
            android:name=".BaoThucReceiver"
            android:exported="false" />

        <receiver
            android:name=".NhacNguReceiver"
            android:exported="false" />

        <receiver
            android:name=".GhiNhanhReceiver"
            android:exported="false" />

        <receiver
            android:name=".KhoiDongReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".QuanTriReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/quan_tri" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
